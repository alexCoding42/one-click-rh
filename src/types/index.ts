export interface IFormInputs {
  theme: string;
  subTheme: string;
  closedRequest: string;
  requestId?: string;
  contactPreference: string;
  otherLinePhoneNumber?: string;
  precision: string;
  hour: string;
}
